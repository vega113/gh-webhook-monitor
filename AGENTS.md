# AGENTS.md

## Worktrees

- Use `/Users/vega/devroot/worktrees` as the default worktree root for this repository.
- Every lane must be created in its own git worktree.
- When lane work is complete, merge it back into `main`, push `origin/main`, and restart the server.
