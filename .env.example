# GitHub webhook secret (set the same value in GitHub repo webhook settings)
WEBHOOK_SECRET=your-secret-here

# Port for the local server
PORT=3847

# Repos to monitor (comma-separated owner/repo)
MONITORED_REPOS=vega113/incubator-wave

# Path to claude CLI
CLAUDE_BIN=claude
